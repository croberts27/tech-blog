<div class="card">
  <h2 class="card-header">Update Post</h2>
  <form class="card-body update-form">

    <div class="form-group">
      <label class="form-label">Title:</label>
      <input
        type="text"
        class="form-input"
        name="post-title"
        value="{{post.title}}"
      />
    </div>

    <div class="form-group">
      <label class="form-label">Content:</label>
      <textarea
        class="form-textarea form-input"
        name="post-content"
      >{{post.content}}</textarea>
    </div>

    <div class="form-group">
      <button type="submit" class="btn btn-primary"><i
          class="bi bi-pencil"
        ></i>Update Post</button>
    </div>

  </form>

</div>